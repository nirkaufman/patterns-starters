// todo: report to GA on every operation
class ShoppingCart {
  addItem(item: any) {}
  checkout() {}
  removeItem(item: any) {}
}

// todo: this is a given 3rd party code. use cache when someone ask for the items;
const state = {
  items: ['item1', 'item2', 'item3'],
}


